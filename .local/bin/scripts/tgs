#!/usr/bin/bash

token=5583657355:AAFzhf-1Gm5s3qV4kn_Ca305ppmeWaL5j-A

# cId
nexsych=2110069054
arpit=1839824891

url="https://api.telegram.org/bot$token/sendMessage"

if [[ ! -z $1 ]] ; then
	if [[ ! -z $2 ]] && [[ -z $3 ]]; then
		cId=${!1}
		msg=$2
	else
		echo "Wrong syntax" ; exit
	fi
else
	cId=$nexsych
	msg=helloFriend
fi

curl -s -X POST $url -d chat_id=$cId -d text="$msg" > /dev/null
